-- Sage Theme — static colors until matugen is configured
hl.config({
    general = {
        col = {
            active_border   = "rgb(7c8cd8)",
            inactive_border = "rgb(c8cad4)",
        },
    },
})
